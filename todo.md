* Make end-to-end unit tests (simulate the bot and make it do things)
* Add idle speed to shooter again
* Handle sticky faults
* Tune drive
* See with one flag if robot is healthy
* Optimize can usage
* Add logging to when a gyro disconnected from our 2 gyro setup like the sparks
* Make vision 0 alloc again
